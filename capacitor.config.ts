import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.entitybible.app',
  appName: 'EntityBible',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
